<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_delivery_base" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.tangerine.delivery.base</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Delivery Integration" string="Delivery Integration" name="delivery_settings" logo="/tangerine_delivery_base/static/description/icon.png">
                    <block title="Webhook Logs" name="webhook_log_setting">
                        <setting string="Auto-delete Webhook Logs"
                                 help="Automatically delete webhook log entries older than the specified number of days. Set 0 to keep logs indefinitely.">
                            <div class="text-muted">
                                Delete logs older than
                                <field name="webhook_log_retention_days" class="o_field_integer oe_inline" style="width: 60px;"/>
                                days
                            </div>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_delivery_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="context">{'module': 'tangerine_delivery_base', 'bin_size': False}</field>
    </record>
</odoo>
